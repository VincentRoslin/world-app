/**
 * Main game view renderer.
 * 3D presentation (Three.js) over the tile-based simulation.
 * Legacy Canvas 2D iso lives unused; swap this re-export only if needed.
 */
export { Renderer } from './Renderer3D';
